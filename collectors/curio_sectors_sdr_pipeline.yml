collector_name: curio_sectors_sdr_pipeline
min_interval: 1m

metrics:
  - metric_name: curio_sectors_sdr_pipeline_total_count
    type: gauge
    help: 'Total number of sectors_sdr_pipeline'
    values: [total_count]
    query: |
      SELECT COUNT(*) AS total_count FROM sectors_sdr_pipeline
